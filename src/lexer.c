#include "lexer.h"
#define NULL 0
Tokens lex(char* input) {
    // split by spaces
    int idx = 0;
    Token* out = malloc(sizeof(Token) * 10);
    while (input[idx] != NULL) {
        switch (input[idx])
        {
        case '+':
            
            break;
        
        default:
            break;
        }
    }
    
}